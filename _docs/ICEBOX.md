# ICEBOX.md — Send in the Clowns
# Purpose: Items parked with an explicit review-or-kill default date (parent doctrine §3.5).
#          Distinct from BACKLOG (which has a promotion CONDITION, not a timer).
# Created: 2026-06-30 | Last Modified: 2026-06-30
# Source: split out of ONE_PAGE_GDD.md "Out of Scope From Day 1" (single-source rule).

---

## I-1. Alt "emotion bar" defeat condition (sad-clown second meter)
A second metering system: a "sad / classically-trained clown" fills a separate emotion bar as an
ALT defeat condition, alongside the primary mirth meter.
**Iceboxed because:** A second metering system before the PRIMARY mirth meter is proven is
premature — it risks muddying the one question Phase 0 exists to answer ("is the read fun?").
**Review-or-kill default:** Revisit only after Phase 0 passes (review ~6 weeks after Phase 0 build
begins, per parent doctrine §3.5; if the mirth meter hasn't proven out by then, this stays frozen
or moves to CUT). Do not build a second meter while the first is unproven.
